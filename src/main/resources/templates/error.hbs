{{#partial "auth"}}
    <main class="mb-auto p-10">
        <div class="flex items-center justify-center w-full">
            <!-- Left Layout (Desktop) -->
            <div class="md:flex md:w-1/3">
                <img src="https://http.cat/{{errorCode}}" alt="401" class="w-full h-full object-cover">
           </div>
    
            <!-- Right Layout (Both Mobile and Desktop) -->
            <div class="md:w-1/2 p-4 ">
                <h1 class="text-4xl md:text-6xl font-bold text-white mb-4">{{error}}</h1>
                <p class="text-lg md:text-xl text-gray-300 mb-8">
                    {{mensaje}}
                </p>
                <a href="/" class="flex flex-col w-2/6 text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded text-lg">
                    Volver a la página principal
                </a>
            </div>
        </div>
    </main>
{{/partial}}
{{> templates/template }}
